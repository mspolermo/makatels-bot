{
    "compilerOptions": {
      "target": "ESNext",             /* Уровень ECMAScript для компиляции вашего кода. */
      "module": "CommonJS",           /* Тип модулей вашего проекта. */
      "strict": true,                 /* Включает строгие проверки типов. */
      "esModuleInterop": true,        /* Разрешает использование импорта CommonJS-модулей в ES модулях. */
      "skipLibCheck": true,           /* Пропуск проверки объявлений типов для сторонних библиотек. */
      "forceConsistentCasingInFileNames": true,  /* Гарантирует согласованный регистр имен файлов. */
      "outDir": "./dist",             /* Каталог для скомпилированных файлов. */
      "rootDir": ".",             /* Каталог с исходными файлами TypeScript. */
      "resolveJsonModule": true,      /* Разрешение импорта JSON как модулей. */
      "baseUrl": ".",                 /* Базовый каталог для разрешения относительных модулей. */
      "paths": {
        "*": [
          "node_modules/*"
        ]
      }
    },
    "include": [
        "*.ts",
        "src/**/*.ts"                  /* Пути к исходным файлам, которые нужно включить в компиляцию. */
    ],
    "exclude": [
      "node_modules"                 /* Пути, которые нужно исключить из компиляции. */
    ]
  }